// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: model_search.sql

package dbgen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const searchModelsByBoolFieldAsc = `-- name: SearchModelsByBoolFieldAsc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::bool IS NULL OR f.bool_value = $5)
  AND ($6::bool IS NULL OR f.bool_value != $6)
ORDER BY
  sf.string_value ASC NULLS LAST,
  sf.numeric_value ASC NULLS LAST,
  sf.int_value ASC NULLS LAST,
  sf.date_value ASC NULLS LAST,
  sf.bool_value ASC NULLS LAST,
  m.created_at DESC
LIMIT $8 OFFSET $7
`

type SearchModelsByBoolFieldAscParams struct {
	FilterField string `json:"filter_field"`
	SortField   string `json:"sort_field"`
	TenantID    string `json:"tenant_id"`
	KindID      string `json:"kind_id"`
	Eq          *bool  `json:"eq"`
	Ne          *bool  `json:"ne"`
	OffsetCount int32  `json:"offset_count"`
	LimitCount  int32  `json:"limit_count"`
}

func (q *Queries) SearchModelsByBoolFieldAsc(ctx context.Context, arg SearchModelsByBoolFieldAscParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByBoolFieldAsc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchModelsByBoolFieldDesc = `-- name: SearchModelsByBoolFieldDesc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::bool IS NULL OR f.bool_value = $5)
  AND ($6::bool IS NULL OR f.bool_value != $6)
ORDER BY
  sf.string_value DESC NULLS LAST,
  sf.numeric_value DESC NULLS LAST,
  sf.int_value DESC NULLS LAST,
  sf.date_value DESC NULLS LAST,
  sf.bool_value DESC NULLS LAST,
  m.created_at DESC
LIMIT $8 OFFSET $7
`

type SearchModelsByBoolFieldDescParams struct {
	FilterField string `json:"filter_field"`
	SortField   string `json:"sort_field"`
	TenantID    string `json:"tenant_id"`
	KindID      string `json:"kind_id"`
	Eq          *bool  `json:"eq"`
	Ne          *bool  `json:"ne"`
	OffsetCount int32  `json:"offset_count"`
	LimitCount  int32  `json:"limit_count"`
}

func (q *Queries) SearchModelsByBoolFieldDesc(ctx context.Context, arg SearchModelsByBoolFieldDescParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByBoolFieldDesc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchModelsByDateFieldAsc = `-- name: SearchModelsByDateFieldAsc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::timestamptz IS NULL OR f.date_value = $5)
  AND ($6::timestamptz IS NULL OR f.date_value != $6)
  AND ($7::timestamptz IS NULL OR f.date_value < $7)
  AND ($8::timestamptz IS NULL OR f.date_value <= $8)
  AND ($9::timestamptz IS NULL OR f.date_value > $9)
  AND ($10::timestamptz IS NULL OR f.date_value >= $10)
ORDER BY
  sf.string_value ASC NULLS LAST,
  sf.numeric_value ASC NULLS LAST,
  sf.int_value ASC NULLS LAST,
  sf.date_value ASC NULLS LAST,
  sf.bool_value ASC NULLS LAST,
  m.created_at DESC
LIMIT $12 OFFSET $11
`

type SearchModelsByDateFieldAscParams struct {
	FilterField string             `json:"filter_field"`
	SortField   string             `json:"sort_field"`
	TenantID    string             `json:"tenant_id"`
	KindID      string             `json:"kind_id"`
	Eq          pgtype.Timestamptz `json:"eq"`
	Ne          pgtype.Timestamptz `json:"ne"`
	Lt          pgtype.Timestamptz `json:"lt"`
	Lte         pgtype.Timestamptz `json:"lte"`
	Gt          pgtype.Timestamptz `json:"gt"`
	Gte         pgtype.Timestamptz `json:"gte"`
	OffsetCount int32              `json:"offset_count"`
	LimitCount  int32              `json:"limit_count"`
}

func (q *Queries) SearchModelsByDateFieldAsc(ctx context.Context, arg SearchModelsByDateFieldAscParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByDateFieldAsc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.Lt,
		arg.Lte,
		arg.Gt,
		arg.Gte,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchModelsByDateFieldDesc = `-- name: SearchModelsByDateFieldDesc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::timestamptz IS NULL OR f.date_value = $5)
  AND ($6::timestamptz IS NULL OR f.date_value != $6)
  AND ($7::timestamptz IS NULL OR f.date_value < $7)
  AND ($8::timestamptz IS NULL OR f.date_value <= $8)
  AND ($9::timestamptz IS NULL OR f.date_value > $9)
  AND ($10::timestamptz IS NULL OR f.date_value >= $10)
ORDER BY
  sf.string_value DESC NULLS LAST,
  sf.numeric_value DESC NULLS LAST,
  sf.int_value DESC NULLS LAST,
  sf.date_value DESC NULLS LAST,
  sf.bool_value DESC NULLS LAST,
  m.created_at DESC
LIMIT $12 OFFSET $11
`

type SearchModelsByDateFieldDescParams struct {
	FilterField string             `json:"filter_field"`
	SortField   string             `json:"sort_field"`
	TenantID    string             `json:"tenant_id"`
	KindID      string             `json:"kind_id"`
	Eq          pgtype.Timestamptz `json:"eq"`
	Ne          pgtype.Timestamptz `json:"ne"`
	Lt          pgtype.Timestamptz `json:"lt"`
	Lte         pgtype.Timestamptz `json:"lte"`
	Gt          pgtype.Timestamptz `json:"gt"`
	Gte         pgtype.Timestamptz `json:"gte"`
	OffsetCount int32              `json:"offset_count"`
	LimitCount  int32              `json:"limit_count"`
}

func (q *Queries) SearchModelsByDateFieldDesc(ctx context.Context, arg SearchModelsByDateFieldDescParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByDateFieldDesc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.Lt,
		arg.Lte,
		arg.Gt,
		arg.Gte,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchModelsByIntFieldAsc = `-- name: SearchModelsByIntFieldAsc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::bigint IS NULL OR f.int_value = $5)
  AND ($6::bigint IS NULL OR f.int_value != $6)
  AND ($7::bigint IS NULL OR f.int_value < $7)
  AND ($8::bigint IS NULL OR f.int_value <= $8)
  AND ($9::bigint IS NULL OR f.int_value > $9)
  AND ($10::bigint IS NULL OR f.int_value >= $10)
ORDER BY
  sf.string_value ASC NULLS LAST,
  sf.numeric_value ASC NULLS LAST,
  sf.int_value ASC NULLS LAST,
  sf.date_value ASC NULLS LAST,
  sf.bool_value ASC NULLS LAST,
  m.created_at DESC
LIMIT $12 OFFSET $11
`

type SearchModelsByIntFieldAscParams struct {
	FilterField string `json:"filter_field"`
	SortField   string `json:"sort_field"`
	TenantID    string `json:"tenant_id"`
	KindID      string `json:"kind_id"`
	Eq          *int64 `json:"eq"`
	Ne          *int64 `json:"ne"`
	Lt          *int64 `json:"lt"`
	Lte         *int64 `json:"lte"`
	Gt          *int64 `json:"gt"`
	Gte         *int64 `json:"gte"`
	OffsetCount int32  `json:"offset_count"`
	LimitCount  int32  `json:"limit_count"`
}

func (q *Queries) SearchModelsByIntFieldAsc(ctx context.Context, arg SearchModelsByIntFieldAscParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByIntFieldAsc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.Lt,
		arg.Lte,
		arg.Gt,
		arg.Gte,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchModelsByIntFieldDesc = `-- name: SearchModelsByIntFieldDesc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::bigint IS NULL OR f.int_value = $5)
  AND ($6::bigint IS NULL OR f.int_value != $6)
  AND ($7::bigint IS NULL OR f.int_value < $7)
  AND ($8::bigint IS NULL OR f.int_value <= $8)
  AND ($9::bigint IS NULL OR f.int_value > $9)
  AND ($10::bigint IS NULL OR f.int_value >= $10)
ORDER BY
  sf.string_value DESC NULLS LAST,
  sf.numeric_value DESC NULLS LAST,
  sf.int_value DESC NULLS LAST,
  sf.date_value DESC NULLS LAST,
  sf.bool_value DESC NULLS LAST,
  m.created_at DESC
LIMIT $12 OFFSET $11
`

type SearchModelsByIntFieldDescParams struct {
	FilterField string `json:"filter_field"`
	SortField   string `json:"sort_field"`
	TenantID    string `json:"tenant_id"`
	KindID      string `json:"kind_id"`
	Eq          *int64 `json:"eq"`
	Ne          *int64 `json:"ne"`
	Lt          *int64 `json:"lt"`
	Lte         *int64 `json:"lte"`
	Gt          *int64 `json:"gt"`
	Gte         *int64 `json:"gte"`
	OffsetCount int32  `json:"offset_count"`
	LimitCount  int32  `json:"limit_count"`
}

func (q *Queries) SearchModelsByIntFieldDesc(ctx context.Context, arg SearchModelsByIntFieldDescParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByIntFieldDesc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.Lt,
		arg.Lte,
		arg.Gt,
		arg.Gte,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchModelsByNumericFieldAsc = `-- name: SearchModelsByNumericFieldAsc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::float8 IS NULL OR f.numeric_value = $5)
  AND ($6::float8 IS NULL OR f.numeric_value != $6)
  AND ($7::float8 IS NULL OR f.numeric_value < $7)
  AND ($8::float8 IS NULL OR f.numeric_value <= $8)
  AND ($9::float8 IS NULL OR f.numeric_value > $9)
  AND ($10::float8 IS NULL OR f.numeric_value >= $10)
ORDER BY
  sf.string_value ASC NULLS LAST,
  sf.numeric_value ASC NULLS LAST,
  sf.int_value ASC NULLS LAST,
  sf.date_value ASC NULLS LAST,
  sf.bool_value ASC NULLS LAST,
  m.created_at DESC
LIMIT $12 OFFSET $11
`

type SearchModelsByNumericFieldAscParams struct {
	FilterField string   `json:"filter_field"`
	SortField   string   `json:"sort_field"`
	TenantID    string   `json:"tenant_id"`
	KindID      string   `json:"kind_id"`
	Eq          *float64 `json:"eq"`
	Ne          *float64 `json:"ne"`
	Lt          *float64 `json:"lt"`
	Lte         *float64 `json:"lte"`
	Gt          *float64 `json:"gt"`
	Gte         *float64 `json:"gte"`
	OffsetCount int32    `json:"offset_count"`
	LimitCount  int32    `json:"limit_count"`
}

func (q *Queries) SearchModelsByNumericFieldAsc(ctx context.Context, arg SearchModelsByNumericFieldAscParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByNumericFieldAsc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.Lt,
		arg.Lte,
		arg.Gt,
		arg.Gte,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchModelsByNumericFieldDesc = `-- name: SearchModelsByNumericFieldDesc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::float8 IS NULL OR f.numeric_value = $5)
  AND ($6::float8 IS NULL OR f.numeric_value != $6)
  AND ($7::float8 IS NULL OR f.numeric_value < $7)
  AND ($8::float8 IS NULL OR f.numeric_value <= $8)
  AND ($9::float8 IS NULL OR f.numeric_value > $9)
  AND ($10::float8 IS NULL OR f.numeric_value >= $10)
ORDER BY
  sf.string_value DESC NULLS LAST,
  sf.numeric_value DESC NULLS LAST,
  sf.int_value DESC NULLS LAST,
  sf.date_value DESC NULLS LAST,
  sf.bool_value DESC NULLS LAST,
  m.created_at DESC
LIMIT $12 OFFSET $11
`

type SearchModelsByNumericFieldDescParams struct {
	FilterField string   `json:"filter_field"`
	SortField   string   `json:"sort_field"`
	TenantID    string   `json:"tenant_id"`
	KindID      string   `json:"kind_id"`
	Eq          *float64 `json:"eq"`
	Ne          *float64 `json:"ne"`
	Lt          *float64 `json:"lt"`
	Lte         *float64 `json:"lte"`
	Gt          *float64 `json:"gt"`
	Gte         *float64 `json:"gte"`
	OffsetCount int32    `json:"offset_count"`
	LimitCount  int32    `json:"limit_count"`
}

func (q *Queries) SearchModelsByNumericFieldDesc(ctx context.Context, arg SearchModelsByNumericFieldDescParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByNumericFieldDesc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.Lt,
		arg.Lte,
		arg.Gt,
		arg.Gte,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchModelsByStringFieldAsc = `-- name: SearchModelsByStringFieldAsc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::text IS NULL OR f.string_value = $5)
  AND ($6::text IS NULL OR f.string_value != $6)
  AND ($7::text IS NULL OR f.string_value ILIKE '%' || $7 || '%')
ORDER BY
  sf.string_value ASC NULLS LAST,
  sf.numeric_value ASC NULLS LAST,
  sf.int_value ASC NULLS LAST,
  sf.date_value ASC NULLS LAST,
  sf.bool_value ASC NULLS LAST,
  m.created_at DESC
LIMIT $9 OFFSET $8
`

type SearchModelsByStringFieldAscParams struct {
	FilterField string  `json:"filter_field"`
	SortField   string  `json:"sort_field"`
	TenantID    string  `json:"tenant_id"`
	KindID      string  `json:"kind_id"`
	Eq          *string `json:"eq"`
	Ne          *string `json:"ne"`
	Contains    *string `json:"contains"`
	OffsetCount int32   `json:"offset_count"`
	LimitCount  int32   `json:"limit_count"`
}

func (q *Queries) SearchModelsByStringFieldAsc(ctx context.Context, arg SearchModelsByStringFieldAscParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByStringFieldAsc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.Contains,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const searchModelsByStringFieldDesc = `-- name: SearchModelsByStringFieldDesc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
JOIN indexed_fields f ON f.model_id = m.id AND f.field_key = $1
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $2
WHERE m.tenant_id = $3
  AND m.kind_id = $4
  AND m.deleted_at IS NULL
  AND ($5::text IS NULL OR f.string_value = $5)
  AND ($6::text IS NULL OR f.string_value != $6)
  AND ($7::text IS NULL OR f.string_value ILIKE '%' || $7 || '%')
ORDER BY
  sf.string_value DESC NULLS LAST,
  sf.numeric_value DESC NULLS LAST,
  sf.int_value DESC NULLS LAST,
  sf.date_value DESC NULLS LAST,
  sf.bool_value DESC NULLS LAST,
  m.created_at DESC
LIMIT $9 OFFSET $8
`

type SearchModelsByStringFieldDescParams struct {
	FilterField string  `json:"filter_field"`
	SortField   string  `json:"sort_field"`
	TenantID    string  `json:"tenant_id"`
	KindID      string  `json:"kind_id"`
	Eq          *string `json:"eq"`
	Ne          *string `json:"ne"`
	Contains    *string `json:"contains"`
	OffsetCount int32   `json:"offset_count"`
	LimitCount  int32   `json:"limit_count"`
}

func (q *Queries) SearchModelsByStringFieldDesc(ctx context.Context, arg SearchModelsByStringFieldDescParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, searchModelsByStringFieldDesc,
		arg.FilterField,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.Eq,
		arg.Ne,
		arg.Contains,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const sortModelsByFieldAsc = `-- name: SortModelsByFieldAsc :many

SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $1
WHERE m.tenant_id = $2
  AND m.kind_id = $3
  AND m.deleted_at IS NULL
ORDER BY
  sf.string_value ASC NULLS LAST,
  sf.numeric_value ASC NULLS LAST,
  sf.int_value ASC NULLS LAST,
  sf.date_value ASC NULLS LAST,
  sf.bool_value ASC NULLS LAST,
  m.created_at DESC
LIMIT $5 OFFSET $4
`

type SortModelsByFieldAscParams struct {
	SortField   string `json:"sort_field"`
	TenantID    string `json:"tenant_id"`
	KindID      string `json:"kind_id"`
	OffsetCount int32  `json:"offset_count"`
	LimitCount  int32  `json:"limit_count"`
}

// Search/sort model queries. There is one filtered+sorted query per
// indexed_fields value type (string/numeric/int/bool/date) plus one
// sort-only pair with no filter join, and each comes in an Asc/Desc variant
// since ORDER BY direction cannot be bound as a parameter in static SQL.
//
// Every query orders by all five typed columns of the sort join (sf) in
// sequence. Because a given field_key is only ever written to one of those
// columns (see internal/indexedfields), the other four are uniformly NULL
// across the whole result set and act as no-op tiebreakers -- so one query
// shape sorts correctly regardless of the sort field's actual type. A
// final m.created_at DESC keeps ordering deterministic when the sort field
// is absent or ties.
func (q *Queries) SortModelsByFieldAsc(ctx context.Context, arg SortModelsByFieldAscParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, sortModelsByFieldAsc,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const sortModelsByFieldDesc = `-- name: SortModelsByFieldDesc :many
SELECT m.id, m.tenant_id, m.kind_id, m.kind_version_id, m.slug, m.body, m.created_at, m.updated_at, m.deleted_at
FROM models m
LEFT JOIN indexed_fields sf ON sf.model_id = m.id AND sf.field_key = $1
WHERE m.tenant_id = $2
  AND m.kind_id = $3
  AND m.deleted_at IS NULL
ORDER BY
  sf.string_value DESC NULLS LAST,
  sf.numeric_value DESC NULLS LAST,
  sf.int_value DESC NULLS LAST,
  sf.date_value DESC NULLS LAST,
  sf.bool_value DESC NULLS LAST,
  m.created_at DESC
LIMIT $5 OFFSET $4
`

type SortModelsByFieldDescParams struct {
	SortField   string `json:"sort_field"`
	TenantID    string `json:"tenant_id"`
	KindID      string `json:"kind_id"`
	OffsetCount int32  `json:"offset_count"`
	LimitCount  int32  `json:"limit_count"`
}

func (q *Queries) SortModelsByFieldDesc(ctx context.Context, arg SortModelsByFieldDescParams) ([]Model, error) {
	rows, err := q.db.Query(ctx, sortModelsByFieldDesc,
		arg.SortField,
		arg.TenantID,
		arg.KindID,
		arg.OffsetCount,
		arg.LimitCount,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Model{}
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.KindID,
			&i.KindVersionID,
			&i.Slug,
			&i.Body,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.DeletedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
